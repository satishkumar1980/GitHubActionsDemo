- name: Check UiPath CLI Installation
  run: |
    if command -v uipcli > /dev/null; then
      echo "UiPath CLI is installed."
      uipcli --version
    else
      echo "UiPath CLI is NOT installed."
      exit 1
    fi
